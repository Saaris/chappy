
Chappy - Overview of the project

Chappy is a fullstack chat application built with React, TypeScript, Node.js, and AWS DynamoDB. The project is structured as a monorepo and includes both frontend and backend.

Technologies and Libraries:

- Frontend: React, TypeScript, Zustand, Vite and CSS.
- Backend: Node.js, Express, TypeScript, AWS (DynamoDB).
- Authentication: JWT (JSON Web Token), bcrypt.
- Other: .env, zod (validation), FontAweSome, and Google Fonts.

API:

- /api/login (POST): User login.
- /api/users (GET, POST, DELETE): Get, create and delete users.
- /api/dm (GET, POST): Get and send direct messages.
- /api/channels (GET, POST, DELETE): Get, create, and delete channels and channel messages.


// Sara Serti Frontend developer
